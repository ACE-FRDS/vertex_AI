{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimum permissions for the Vertex AI management window.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:allow-open"]
}
