# ADR-005: Vertex Context Protocol

Status: Accepted; v0.1 baseline implemented

Use a versioned provider-neutral context envelope. Context is selected per task and target location after scope/privacy filtering and hard budgeting. Core generation accepts only a prepared-context capability produced by the builder; adapters perform final provider conversion.
