# ADR-001: Rust Core

Status: Accepted

Use a headless Rust workspace for Core and Edge-shared foundations. Rust supports explicit boundaries, predictable deployment, and failure-safe ownership. UI technology does not enter Core crates.

